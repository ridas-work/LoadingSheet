# Plan 02 — Summary

## Done
- `restoreGoodReturnsAfterPartialDelivery` — restores good returns after partial close (capped at deducted qty).
- `applyLateReturnStock` — adds good bottles to Rashid stock with **no cap** (months-later returns).
- `applyDeliveryClosureStock` — orchestrates partial close stock restore.
